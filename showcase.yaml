type: runtime

work_dir: .

startup: |
  pip install -r requirements.txt
entrypoint: gunicorn main:app

envs:
  PYTHONPATH: /srv:$PYTHONPATH
http_proxy: 8000
https: on

use_mariadb: true

branch:
  main:
