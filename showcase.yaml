type: runtime

work_dir: .

startup: |
  pacman -S mariadb-libs
  pip install -r requirements.txt
entrypoint: gunicorn main:app

envs:
  PYTHONPATH: "/srv"
http_proxy: 8000
https: on

use_mariadb: true

branch:
  main:
