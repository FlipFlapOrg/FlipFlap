type: runtime
__image: python:3.8-alpine

work_dir: .

startup: |
  apk add  --no-cache build-base mariadb-dev
  pip install -r requirements.txt
entrypoint: gunicorn main:app

envs:
  PYTHONPATH: "/srv"
http_proxy: 8000
https: on

use_mariadb: true

branch:
  main:
